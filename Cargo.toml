[package]
name = "cube"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

# Disable wasm-opt optimization to avoid download failures in restricted environments
[package.metadata.wasm-pack.profile.release]
wasm-opt = false

[dependencies]
glam = "0.30.9"
wasm-bindgen = "0.2.106"
